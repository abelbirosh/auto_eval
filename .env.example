# Copy to .env and fill in. .env is gitignored; never commit a real key.

# API key for the classifier.
OPENAI_API_KEY=

# Optional. Override the endpoint and model the classifier uses.
# AUTO_EVAL_BASE_URL=
# AUTO_EVAL_MODEL=

# Optional. A stronger model for the ground-truth web search alone.
# Unset, it uses AUTO_EVAL_MODEL.
# AUTO_EVAL_SEARCH_MODEL=

# Optional. The model a run puts under test, and the model that rules on the
# rubrics. Keep them different: a model grading its own work has a documented
# self-preference bias, and `auto-eval run` warns when they match.
# AUTO_EVAL_SUBJECT_MODEL=
# AUTO_EVAL_JUDGE_MODEL=

# Optional. Where `auto-eval run` writes results and the dashboard reads them.
# AUTO_EVAL_RUNS_DIR=runs
